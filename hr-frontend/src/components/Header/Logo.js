import React from 'react';
import PropTypes from 'prop-types';

const Logo = props => {
    return (
        <div className="header-logo header-logo-effect">
           <p>TEST </p>
        </div>
    );
};

Logo.propTypes = {

};

export default Logo;