import React from 'react';
import PropTypes from 'prop-types';

const Logo = props => {
    return (
        <div className="header-logo">
           <p>Logo </p>
        </div>
    );
};

Logo.propTypes = {

};

export default Logo;